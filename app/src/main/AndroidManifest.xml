<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ps.aio.gazacars">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GazaCars">
        <activity android:name=".ui.activities.CarDetailsActivity"/>
        <activity
            android:name=".ui.activities.IntroActivity"
            android:theme="@style/Theme.GazaCars.IntroActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:theme="@style/Theme.GazaCars.MainActivity" >
        </activity>
        <activity
            android:name=".ui.activities.RegisterActivity"
            android:label="@string/register"
            android:parentActivityName=".ui.activities.LoginActivity" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:label="@string/login" />
        <activity
            android:name=".ui.activities.SplashActivity"
            android:theme="@style/Theme.GazaCars.SplashActivity"/>
    </application>

</manifest>